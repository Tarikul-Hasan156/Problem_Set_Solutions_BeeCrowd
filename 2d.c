#include<stdio.h>
int main ()
{
    char arr[20];
    gets (arr);
    int i;
    for  (i=0;i<x;i++)
    {
        if (arr[i]>='a'&&arr[i]<='z')
        {
            arr[i]=arr[i]-32;
        }
       // printf("The string is : %s",arr);
        
    }
     printf("The string is : %s",arr);
     getch();
}